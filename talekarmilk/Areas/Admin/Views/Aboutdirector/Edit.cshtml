@model talekarmilk.Models.TblAboutDirector

@{
    ViewBag.Title = "Edit";
    
}

<h2>Edit</h2>

@using (Html.BeginForm(Html.BeginForm(null, null, FormMethod.Post, new { enctype = "multipart/form-data" })))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>TblAboutDirector</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.dir_id)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.dir_id)
            @Html.ValidationMessageFor(model => model.dir_id)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.dir_name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.dir_name)
            @Html.ValidationMessageFor(model => model.dir_name)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.dir_content)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.dir_content)
            @Html.ValidationMessageFor(model => model.dir_content)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.dir_image)
        </div>
        <div class="editor-field">
            <input type="file" name="file" id="file" required="required" />
            @Html.ValidationMessageFor(model => model.dir_image)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
