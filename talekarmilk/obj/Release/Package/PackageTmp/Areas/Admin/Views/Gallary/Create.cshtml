@model talekarmilk.Models.Tbl_Gallary

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm(Html.BeginForm(Html.BeginForm(null, null, FormMethod.Post, new { enctype = "multipart/form-data" })))) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Tbl_Gallary</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.img_id)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.img_id)
            @Html.ValidationMessageFor(model => model.img_id)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.img_title)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.img_title)
            @Html.ValidationMessageFor(model => model.img_title)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.img_image)
        </div>
        <div class="editor-field">
            <input type="file" name="file" id="file" required="required" />
            @Html.ValidationMessageFor(model => model.img_image)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.img_content)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.img_content)
            @Html.ValidationMessageFor(model => model.img_content)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
